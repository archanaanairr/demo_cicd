name: Python CI and CD

on: [push]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Run tests
        run: python test_app.py

      - name: Deploy (Mock)
        if: success()    # This means only run if tests pass
        run: echo "Deployment successful! 🚀"
